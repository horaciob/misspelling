#!/usr/bin/env ruby
# frozen_string_literal: true
require 'byebug'

$LOAD_PATH.unshift("#{__dir__}/../lib")

require 'misspelling'

runner = Misspelling::Runner.new(options:'la')
runner.start
runner.show_result

